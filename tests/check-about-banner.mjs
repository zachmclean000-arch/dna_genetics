import {chromium} from '@playwright/test';import assert from 'node:assert/strict';const b=await chromium.launch({channel:'msedge'});try{const p=await b.newPage();for(const width of [1440,768,390]){await p.setViewportSize({width,height:1000});await p.goto('http://127.0.0.1:5173/about');await p.evaluate(()=>document.fonts.ready);const hero=p.locator('.dna-about-hero');const m=await hero.evaluate(el=>{const h=el.querySelector('h1'),r=el.getBoundingClientRect(),t=h.getBoundingClientRect(),i=el.querySelector('img');return {height:r.height,font:getComputedStyle(h).fontSize,top:t.top-r.top,left:t.left-r.left,imageHeight:i.getBoundingClientRect().height,gold:getComputedStyle(h.querySelector('span')).color,loaded:i.complete&&i.naturalWidth>0};});assert.equal(m.height,width<=600?220:150);assert.equal(m.font,width<=600?'45px':width<=991?'42px':'50px');assert.equal(m.top,width<=991?30:50);assert.equal(m.gold,'rgb(255, 203, 0)');assert.equal(m.loaded,true);if(width<=991)assert.equal(m.imageHeight,250);if(width<=600)assert.equal(m.left,15);await hero.screenshot({path:`reference/comparison/about-banner-fixed-${width}.png`});console.log(width,m);}}finally{await b.close();}
