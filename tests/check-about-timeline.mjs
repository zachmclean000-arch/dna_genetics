import { chromium } from '@playwright/test';import assert from 'node:assert/strict';const b=await chromium.launch({channel:'msedge'});try{const p=await b.newPage();for(const width of [1440,768,390,320]){await p.setViewportSize({width,height:1000});await p.goto('http://127.0.0.1:5173/about');await p.evaluate(()=>document.fonts.ready);assert.equal(await p.locator('.dna-about-timeline li').count(),19);assert.ok(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));const d=await p.locator('.dna-about-history').evaluate(el=>{const img=el.querySelector('.dna-about-section-heading>img').getBoundingClientRect(),h=el.querySelector('h2').getBoundingClientRect(),li=el.querySelector('li'),year=li.querySelector('span').getBoundingClientRect(),text=li.querySelector('h3').getBoundingClientRect();return {stacked:h.y>img.bottom,indent:text.x-year.x,font:getComputedStyle(li.querySelector('h3')).fontSize,color:getComputedStyle(li.querySelector('h3')).color};});assert.ok(d.stacked);assert.equal(d.indent,width<=600?117:160);assert.equal(d.color,'rgb(11, 77, 60)');await p.screenshot({path:`reference/comparison/about-timeline-fixed-${width}.png`});}console.log('Timeline heading, marker alignment, milestones and responsive overflow checks passed.');}finally{await b.close();}
